<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">semester_projekt</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="koordinator">
                <span class="oi oi-plus" aria-hidden="true"></span> Koordinator side
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="bruger">
                <span class="oi oi-plus" aria-hidden="true"></span> Bruger side
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="nyvagt">
                <span class="oi oi-plus" aria-hidden="true"></span> Opret ny vagt
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="nybruger">
                <span class="oi oi-plus" aria-hidden="true"></span> Opret ny bruger
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="nyopgave">
                <span class="oi oi-plus" aria-hidden="true"></span> Opret ny opgave
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
